
<l-button bindlintap="onCopyValue" 
          l-class="{{btn == true ? 'btn' : ''}}" 
          width="70" height="40" size="mini" 
          bg-color="{{color}}">
          <text class="btn-text">{{text}}</text>
</l-button>
