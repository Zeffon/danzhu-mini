<view class="container">
  <block wx:for="{{_remark}}" wx:key="index">
    <l-input bind:lininput="onInput" bind:linclear="onClear" tip-type="toast" label="{{item.key}}:" id="{{index}}"
      value="{{item.value}}" required="{{true}}" rules="{{valueRule}}" clear="{{true}}" width="750" placeholder="请输入...">
    </l-input>
  </block>
</view>

<l-toast></l-toast>